# test model Agent Instructions

## Responsibility

`test/api/model` verifies domain model invariants and value object behavior.

## Rules

- Keep tests deterministic and free of framework dependencies.
- Cover role checks, link models, QR result state, upload result values, and status behavior when implemented.

## Warnings

- Do not add generated DTO tests here.
